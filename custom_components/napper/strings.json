{
  "config": {
    "step": {
      "user": {
        "title": "Napper Baby Sleep Tracker",
        "description": "Enter your baby ID (from config.json). API token is optional — leave blank for local-only mode. Add the token later to enable Napper app sync.",
        "data": {
          "api_token": "API Token (JWT)",
          "baby_id": "Baby ID"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid token or baby ID. The token may have expired."
    }
  }
}
